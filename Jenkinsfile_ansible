pipeline {
    agent {
        docker {
            image 'ansible/ansible'
        }
    }
    stages {
        stage('version') {
            steps {
                sh 'ansible --version'
            }
        }
    }
}
