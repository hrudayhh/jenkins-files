pipeline {
    agent {
        docker {
            image 'hrudayhh/ansible:v2.9.7'
            args '-u root'
        }
    }
    stages {
        stage('version') {
            steps {
                sh 'ansible --version'
                
            }
        }
    }
}
