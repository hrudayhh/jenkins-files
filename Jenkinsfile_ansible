pipeline {
    agent {
        docker {
            image 'maven:3-alpine'
        }
    }
    stages {
        stage('version') {
            steps {
                //sh 'ansible --version'
                sh 'mvn --version'
            }
        }
    }
}
