pipeline {
    agent {
        docker {
            image 'hrudayhh/ansible:v2.9.7'
        }
    }
    stages {
        stage('version') {
            steps {
                sh 'ansible --version'
            }
        }
    }
}
