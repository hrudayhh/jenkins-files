pipeline {
    agent {
        docker {
            image 'ansible/ansible:ubuntu1604py3'
        }
    }
    stages {
        stage('version') {
            steps {
                sh 'ansible --version'
            }
        }
    }
}
